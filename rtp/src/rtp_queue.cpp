#include "rtp_queue.h"

namespace huoguo {
namespace rtp {

int RtpQueue::push(std::shared_ptr<RtpPacket> packet) {
//     uint16_t sequence_number = packet->get_sequence_number();
//     size_t index = sequence_number % m_queue.size();
//     if (!m_first_packet_received) {
//         m_first_sequence_number = packet->
//         m_first_packet_received = true;
//     }
// }
    return 0;
}

}
}